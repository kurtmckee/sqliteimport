name: "✨ Prep release"

on:
  workflow_dispatch:
    inputs:
      version:
        description: "The new version"
        type: "string"
        required: true

jobs:
  prep-release:
    name: "Prep release v${{ inputs.version }}"

    permissions:
      contents: "write"
      pull-requests: "write"

    strategy:
      matrix:
        include:
          - tox-label-create-changes: "prep-release"
            branch-name: "release/$VERSION"
            commit-title: "Update project metadata"
            pr-base: "releases"
            pr-title: "Release v$VERSION"

    uses: "kurtmckee/github-workflows/.github/workflows/create-pr.yaml@598fc01db95578db9417cde9338ae7e7d891e66b" # v2.1
    with:
      config: "${{ toJSON(matrix) }}"
      version: "${{ inputs.version }}"
