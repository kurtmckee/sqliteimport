[lint.isort.sections]
sqliteimport = ["sqliteimport"]

[lint.isort]
section-order = [
    "future",
    "standard-library",
    "sqliteimport",  # <-- Add this above 'third-party'
    "third-party",
    "first-party",
    "local-folder",
]
